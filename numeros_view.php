<?php
// This script and data application were generated by AppGini 23.15
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/numeros.php');
	include_once(__DIR__ . '/numeros_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('numeros');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'numeros';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`numeros`.`curul`" => "curul",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`numeros`.`curul`',
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`numeros`.`curul`" => "curul",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`numeros`.`curul`" => "Curul",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`numeros`.`curul`" => "curul",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`numeros` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'numeros_view.php';
	$x->TableTitle = 'Numeros';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`numeros`.`curul`';

	$x->ColWidth = [150, ];
	$x->ColCaption = ['Curul', ];
	$x->ColFieldName = ['curul', ];
	$x->ColNumber  = [1, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/numeros_templateTV.html';
	$x->SelectedTemplate = 'templates/numeros_templateTVS.html';
	$x->TemplateDV = 'templates/numeros_templateDV.html';
	$x->TemplateDVP = 'templates/numeros_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: numeros_init
	$render = true;
	if(function_exists('numeros_init')) {
		$args = [];
		$render = numeros_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: numeros_header
	$headerCode = '';
	if(function_exists('numeros_header')) {
		$args = [];
		$headerCode = numeros_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: numeros_footer
	$footerCode = '';
	if(function_exists('numeros_footer')) {
		$args = [];
		$footerCode = numeros_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
